---
import type { RichTextBlock } from '../../lib/blocks/types'
import { tiptapToHtml } from '../../lib/blocks/tiptap-to-html'

interface Props {
	block: RichTextBlock
}

const { block } = Astro.props

const html = tiptapToHtml(block.data.content)
---

<div class="richtext-block" set:html={html} />
